services:
  api:
    build: .
    container_name: api-aula
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    command: sh -c "npm install && npm run start:dev"
